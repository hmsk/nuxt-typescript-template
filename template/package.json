{
  "name": "{{ name }}",
  "description": "{{ description }}",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "nuxt": "^2.3.4",
    "nuxt-property-decorator": "^1.3.1",
    "nuxt-ts": "^0.2.0"
  },
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate"
  },
  "devDependencies": {
    "fork-ts-checker-webpack-plugin": "^0.5.3",
    "ts-loader": "^5.3.2",
    "typescript": "^3.2.2"
  }
}
